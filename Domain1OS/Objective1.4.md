# 1.4 Given a scenario, use Microsoft Windows operating system features and tools.
## Task Manager
### Services
Shows and manages background services running in Windows.
````bash
Example: Restarting the Print Spooler service.
````
### Startup
Controls programs that launch automatically during boot.
````bash
Example: Disabling Spotify from startup to improve boot speed.
````
### Performance
Displays CPU, RAM, disk, and network usage in real time.
````bash
Example: Checking CPU usage during system lag.
````
### Processes
Shows currently running applications and background processes.
````bash
Example: Ending a frozen Chrome process.
````
### Users
Displays users currently logged into the system and their resource usage.
````bash
Example: Logging off another inactive user session.
````
## Microsoft Management Console (MMC) snap-in
### Event Viewer (eventvwr.msc)
Displays system logs and error events for troubleshooting.
````bash
Example: Checking crash logs after a blue screen.
````
### Disk Management (diskmgmt.msc)
Manages partitions, volumes, and storage drives.
````bash
Example: Creating a new partition on an SSD.
````
### Task Scheduler (taskschd.msc)
Automates tasks to run at specific times or events.
````bash
Example: Scheduling automatic disk cleanup weekly.
````
### Device Manager (devmgmt.msc)
Manages hardware devices and drivers.
````bash
Example: Updating a network adapter driver.
````
### Certificate Manager (certmgr.msc)
Manages digital certificates used for security and authentication.
````bash
Example: Viewing installed SSL certificates.
````
### Local User and Groups (lusrmgr.msc)
Manages local user accounts and group permissions.
````bash
Example: Creating a new local administrator account.
````
### Performance Monitor (perfmon.msc)
Tracks detailed system performance statistics over time.
````bash
Example: Monitoring RAM usage trends on a server.
````
### Group Policy Editor (gpedit.msc)
Configures advanced Windows settings and restrictions.
````bash
Example: Blocking access to Control Panel for users.
````
## Additional tools
### System Information (msinfo32. exe)
Displays detailed information about hardware, drivers, and system configuration.
````bash
Example: Checking BIOS version and installed RAM.
````
### Resource Monitor (resmon.exe)
Provides detailed monitoring of CPU, memory, disk, and network activity.
````bash
Example: Finding which app is using excessive disk activity.
````
### System Configuration (msconfig. exe)
Manages startup settings and boot troubleshooting options.
````bash
Example: Booting Windows into Safe Mode.
````
### Disk Cleanup (cleanmgr.exe)
Removes temporary and unnecessary files to free storage space.
````bash
Example: Deleting Windows temporary update files.
````
### Disk Defragment (dfrgui.exe)
Optimizes fragmented hard drives to improve performance.
````bash
Example: Defragmenting an older HDD.
````
### Registry Editor (regedit.exe)
Allows viewing and editing of the Windows Registry database.
````bash
Example: Modifying registry settings to fix a software issue.
````
